<!DOCTYPE html>
<html>
  <head>

    <title>COFFEEMANIA</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="shadow-sm">
      <nav class="navbar navbar-expand-lg bg-dark">
        <div class="container">
          <h2 class="text-light">COFFEEMANIA</h2>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">

            <% if user_signed_in? %>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'マイページ', user_path(:id), class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'カフェ投稿一覧', cafes_path, class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'コーヒー豆投稿一覧', beans_path, class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'nav-link text-light' %></li>
            <% else %>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to '新規登録', new_user_registration_path, class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'ログイン', new_user_session_path, class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'About', about_path, class: 'nav-link text-light' %></li>
              <li class="nav-item m-1 rounded border border-muted"><%= link_to 'Top', root_path, class: 'nav-link text-light' %></li>
            <% end %>
            </ul>
          </div>
        </div>
    </header>
    <main class="container mt-5">
    <%= yield %>
    </main>
  </body>
  <footer class="mt-auto py-3 bg-dark">
　    <div class="text-center text-light">Coffee makes you Happy</div>
　</footer>
</html>
