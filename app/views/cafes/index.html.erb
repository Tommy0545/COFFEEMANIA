<h1>
  <b>
    Cafe Post List
  </b>
</h1>
<table class="table" >
  <thead>
    <tr>
      <th>User</th>
      <th>Store Name</th>
      <th>Location</th>
      <th>Comment</th>
      <th>♡</th>
    </tr>
  </thead>
  <tbody>
    <% @cafes.each do |cafe| %>
      <tr>
        <td><%= link_to cafe.user.user_name, user_path(cafe.user_id) %></td>
        <td><%= link_to cafe.store_name,cafe_path(cafe.id) %></td>
        <td><%= cafe.location %></td>
        <td><%= link_to "#{cafe.cafe_comments.count} Comment", cafe_path(cafe.id) %></td>
        <td><%= cafe.cafe_favorites.count %></td>
      </tr>
    <% end %>
  </tbody>

</table>


